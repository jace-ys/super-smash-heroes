version: "3.7"

services:
  service.superhero:
    ports:
      - "5001:8080"

  service.battle:
    ports:
      - "5002:8080"

  db.postgres.init:
    volumes:
      - ./test/data/postgres/migrations:/postgres/migrations
